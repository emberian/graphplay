# Vacuity / stub-driven-green audit — Dowsing + Towers 6/7 slice

Read-only audit. Slice: `Graphplay/Dowsing/{ChiralBundlePST, ChiralGraphon,
FractionalRevivalNC, NoiseEquitable, FilteredColimitPST, Conjecture93,
WeakCoupling}.lean` + `Graphplay/Tower6.lean`, `Graphplay/Tower7.lean`.

Categories: **A** placeholder def, **B** trivial conclusion, **C** vacuous
hypothesis, **D** stub-driven green vacuity (def stub ⇒ theorem hollow), **E**
false-as-stated, **F** degenerate quantification.

Worst-first (D, A on top). Towers 6/7 honest-`sorry`s are NOT findings per the
mandate; only stubs-presented-as-real and green-but-hollow theorems are flagged.

| file:line | cat | name | why | suggested fix |
|---|---|---|---|---|
| Tower7.lean:222 | **D/A** | `CoherentEquitablePartition.quotient` | Body is `⟨X, A⟩`: the "quotient" of *any* coherent equitable partition is literally the host object `X` with the host endo `A`. The whole partition argument `_` is discarded. This is a stub presented as "the truncated quotient". | Either mark clearly as a degenerate `p = 1` specialization in the *name*, or build the colimit object (deferred-sorry) instead of returning the input. |
| Tower7.lean:285 | **D** | `infinity_pst_lift` | Headline ∞-PST lift. "Proven" by `intro h; exact h` — only because `(P.quotient).2 = A` (the stub above) makes hypothesis and conclusion the *literally same* proposition. Looks like a lift theorem; transports nothing. | Real content needs a non-identity quotient; as written it is `P → P`. Downgrade to `sorry` or annotate as vacuous. |
| Tower7.lean:259 | **A/E** | `IsInfinityPST` | Reduced to `Nonempty (Hom i j) ∧ Nonempty (Hom j i)` — object equivalence, independent of `A` and `τ`. Self-described "faithful finite shadow", but it drops all PST/evolution/time content, so `A`,`τ` are dead args. Enables the hollow `infinity_pst_lift`. | Acceptable as a placeholder only if downstream theorems are not presented as genuine PST lifts. Flag the unused `_A`,`_τ`. |
| Tower7.lean:602 | **B/F** | `TopologicalInvarianceConjecture` | `:= ∀ (g : ℕ) (_ : g ≥ 0), True`. `g ≥ 0` is always true for ℕ and the body is `True`: the "conjecture" is `True`. | Replace with the real quantifier over surfaces/embeddings/mixing-time, or mark as placeholder Prop. |
| Tower7.lean:611 | **B/D** | `topological_invariance_corollary` | Proves the above by `intro _ _; trivial`. Green only because target is `True`. Docstring claims it is a corollary of `infinity_pst_lift`. | Tie to a real statement once `TopologicalInvarianceConjecture` is real. |
| Tower7.lean:443/462 | **A/B** | `DerivedHermPart.Ext0`, `pst_as_Ext0` | `Ext0 _ _ _ := Unit`; `pst_as_Ext0` concludes `Nonempty Unit` via `⟨()⟩`. The "PST ⟺ invertible Ext⁰ class" content is absent (Unit is always inhabited). | Honest-`sorry` placeholder type is fine, but `pst_as_Ext0` is a tautology (`Nonempty Unit`); state nothing or defer. |
| Tower7.lean:511 | **B** | `mtc_correspondence` | Concludes `Nonempty MTC` where `MTC := { dummy : Unit }`; proven `⟨{dummy:=()}⟩`. Always inhabited; carries no correspondence. | Defer or give MTC real data. |
| Tower7.lean:404 | **B** | `coherent_quasi_infinite_limit` | Conclusion `True` (`by trivial`). Honest scaffold but presented in inventory as "stated". | Fine as scaffold; not a theorem. |
| Tower6.lean:680 | **A** | `CellUniformSubalgebra` | `:= { _x | True }` — the whole carrier. Presented as "the subalgebra generated by cell-indicator-lifts". | Build the genuine generated subalgebra or mark stub in name. |
| Tower6.lean:923 | **A/E** | `StalkwisePST` | Defined as `∀ x, ∃ k, (k = i ∨ k = j) ∨ x ∈ cover k`. The second disjunct `x ∈ cover k` is satisfiable for *every* `x` (cover covers ⊤), so this is essentially always true and says nothing about PST/evolution. Self-labeled "weaker covering condition" but named `StalkwisePST`. | Rename to reflect it is a covering predicate, not PST; or defer with sorry. |
| Tower6.lean:939 | **D** | `DenseOpenPST` | `∃ U, Dense U ∧ StalkwisePST …` — built on the hollow `StalkwisePST`; `U=⊤` always works. | Same as above. |
| Tower6.lean:949 / 1097 / 1114 | **D** | `stalkwisePST_implies_denseOpenPST`, `open_direction_cohomology`, `open_direction_tqc` | All three are real-looking implications proven trivially because `DenseOpenPST` collapses (take `U=⊤`) onto the hollow `StalkwisePST`. Green but vacuous. | Will become genuine once the two PST predicates carry evolution content. |
| Tower6.lean:416/429, 884 | **A** | `ofGraphon`, `heawoodEnvelopeSheaf` | `adj := (0 : ℂ)` / `(0 : Matrix (Fin 1)(Fin 1) ℂ)` placeholders standing in for `W.op` / Heawood adjacency. Honest comments admit the Mathlib gap, but these are presented as the "Tower-4/Heawood recovery example" objects. | Acceptable placeholders; ensure no downstream theorem reads them as the real operator. |
| Tower6.lean:438/477/665/693/710/788/893/966 | **B** | `tower4_recovery`, `tower5_recovery`, `restrict_factors_through_cell`, `sheaf_spectral_lift`, `spectrum_subset_cellUniform`, `locallyFinite_uniform_cells`, `heawoodEnvelope_chromatic_bound`, `robust_pst_neighbourhood` | All conclude `True` (or `∃…, True`). Honest scaffolding, flagged here for the record; not substantive theorems. | Defer; not findings beyond scaffold. |
| Tower6.lean:779 | **A** | `LocallyFinite` | `:= True`. Predicate that holds for every sheaf graph; used as a hypothesis in `locallyFinite_uniform_cells`. | Vacuous hypothesis (C) wherever consumed. |
| FilteredColimitPST.lean:747 | **D/A** | `InversePartitionSequence.quotient` | Body `fun _ _ => 0` — the entire inverse-limit quotient matrix is the zero matrix, independent of `𝒮`. Presented as "the stage-`n` quotient". | Build the real pullback quotient or mark stub; currently any spectral claim about it is about `0`. |
| FilteredColimitPST.lean:761 | **D** | `InversePartitionSequence.pst_lifted` | Hypothesis `∀ n, IsPST_finite (𝒮.quotient n) …` is about the zero-matrix quotient; conclusion just re-returns `h_pst` (`:= h_pst`). Green tautology `P → P` over a stub-valued predicate. | Real cofiltered lift needed; as written it transports the (vacuous-on-stub) hypothesis to itself. |
| FilteredColimitPST.lean:810 | **B/C** | `ConsistentPartitionSequence.pst_rate_tradeoff` | Conclusion `∃ C, 0 < C ∧ C ≥ T`, proven `⟨T, hT, le_refl T⟩`. The "rate-vs-time tradeoff" / big-O bound is absent; this just re-exhibits `T`. The `α`,`hα` args are dead. | State the actual `O(T/n^α)` bound or remove the misleading docstring. |
| Conjecture93.lean:181 | **A** | `singleCellCompleteSeqFin1` (and `singleCellCompleteSeq`) | Concrete builder is fine, BUT see below — every named "test family" is realized as this single-cell complete-graph sequence rather than its claimed graph. | n/a (the builder itself is honest). |
| Conjecture93.lean:542/580/768 | **A/E** | `Hammingq`, `Kn_plus_pathn`, `cayleyS_n` | Hamming `H(n,q)`, Xie–Tamon `K_n+path_n`, and Cayley(S_n) are ALL realized as the *same* `singleCellCompleteSeqFin1` (complete graph `K_{q^n}` / `K_{2n+2}` / `K_{(n+1)!}`) — none is its actual graph. The distinguishing structure (distance partition, tridiagonal tail, conjugacy classes) is absent. | These are vertex-count-only stand-ins; the "family" claims overstate the model. Annotate as cardinality skeletons. |
| Conjecture93.lean:515/550/588/693/731/777 | **D** | `Kn_constSign_…`, `Hammingq_…`, `Kn_plus_pathn_…`, `K4multipartite_…`, `heawoodEnvelope_…`, `cayleyS_n_satisfies_conj93` | Each "test family satisfies Conjecture 9.3" is proven by `conjecture93_weak_iff _`, which itself rests on the two **sorried** halves (`conjecture93_weak_forward/reverse`, lines 367/382). So the six "satisfies" theorems are green only because the underlying iff is asserted; they verify nothing family-specific. | These are not independent confirmations; they inherit the unproven iff. Worth noting in the conjecture's evidence table. |
| ChiralBundlePST.lean:492 | **B/F** | `levine_base_corollary` | Genuinely proved, but the content is PST on a one-vertex (`Unit`) quotient whose adjacency is `0` ⇒ evolution is `1` ⇒ `‖1‖=1` trivially. Degenerate single-point quotient; the "Levine speedup" is not in this statement. | Honest (docstring admits triviality); flagged as degenerate-domain, low severity. |
| ChiralBundlePST.lean:662 | **C/B** | `signedBy_phaseRefined_equitable` | Claims a phase-refined equitable partition exists; "proof" returns the **discrete** partition `EquitablePartition.discrete (G.signedBy s)`, which is equitable for *every* graph. The phase-refinement content (the point of the theorem) is bypassed. | Statement `∃ I', Nonempty (EquitablePartition …)` is too weak to capture refinement; strengthen to assert the refined cells, or it is vacuously witnessed. |
| ChiralGraphon.lean:768 | **A** | `U1Gauge.chiralContent` | Docstring calls it a "placeholder constant" but the body is the genuine `∑ Re(τ τ τ)` — actually fine. No finding; noted because the comment says "placeholder". | None (def is real; fix the stale comment). |

## Clean files (no vacuity/stub findings)

- **FractionalRevivalNC.lean** — `IsFR`, `IsCellUniformFR`, `IsNCFR`, `Graphon.IsFR`,
  `cycleGraph`, `hammingGraph`, `chiralKn` are all genuine; theorems with proofs
  (`isPST_of_isFR_pst`, `swap`, `ncfr_commutative_reduction`, `isFR_trivial`) carry
  real hypotheses and nontrivial conclusions. Remaining items are honest `sorry`s or
  genuinely-stated open `Prop`s. The earlier `+ 0`/tautology issues are documented as
  already-corrected. `isFR_trivial` (τ=0, α=1, β=0) is a labeled boundary case, not vacuous.
- **NoiseEquitable.lean** — substantively proven (`commutantOf_preservesCellUniform`,
  `mem_centerMat_iff`, `breakingScore_zero_iff_blockDiagonal`, `boundaryDephasing_*`,
  `noise_assisted_speedup_conjecture` with the added non-degenerate `hxy` hypothesis,
  the sentinel lemmas). Documents and *fixes* the false `BreakingScore=0 ↔
  cellUniformSymmetric` (issue #53). `markedRefined` honestly returns the embedded
  (not split) partition. Clean.
- **WeakCoupling.lean** — `CoupledSystem`, `fullHam`, `feshbachMap`, `selfEnergy`,
  `tRex*`, `GammaCospectral` are all genuine; proven lemmas (`fullHam_isHermitian`,
  `fullHam_decoupled_at_zero`, `feshbachMap_at_zero`, `tRexCoupling_symm`,
  `fullHam_no_cross_edges_at_zero`) are nontrivial. The two headline PST theorems are
  honest `sorry`s with satisfiable hypotheses. Clean.
- **ChiralBundlePST.lean** (rest) — `totalChiralSigning`, `quotientSigned`,
  `fiberPartitionSigned` (large genuine equitability proof), `completeFiberChiralBundle`
  and the example bundles are real constructions; headline theorems are honest `sorry`s.
  Only the two degenerate items above flagged.
- **ChiralGraphon.lean** — `signedBy`, `constantChiral`, `iteratedHammingChiral`,
  `GraphonSigning`, `CellCrossConstant`, `quotientPhase` all genuine with real proofs
  (`signedBy_trivial`, `constantChiral` herm/bounded/loopless, the `_limit`/`_mixing`
  shadow lemmas). The `quotientPhase_herm/_unimod` sorries are honestly explained as
  needing a positive-cell-measure hypothesis (real obstruction, not vacuity). Clean.

## Summary

- **Total findings: 23 rows** (several rows group multiple `True`-conclusion scaffolds).
- Per category (primary tag):
  - **D (stub-driven green vacuity): 7** — Tower7 `infinity_pst_lift`+`quotient`,
    Tower6 `DenseOpenPST`+3 implications, FilteredColimitPST `pst_lifted`+`quotient`,
    Conjecture93 six "satisfies" theorems.
  - **A (placeholder def): 8** — Tower7 `quotient`/`Ext0`, Tower6
    `CellUniformSubalgebra`/`StalkwisePST`/`LocallyFinite`/`ofGraphon`/`heawoodEnvelopeSheaf`,
    FilteredColimitPST `InversePartitionSequence.quotient`, Conjecture93 family realizations.
  - **B (trivial conclusion): 6** — Tower7 `TopologicalInvarianceConjecture`+corollary,
    `pst_as_Ext0`, `mtc_correspondence`, `coherent_quasi_infinite_limit`; Tower6 `True`-block;
    FilteredColimitPST `pst_rate_tradeoff`.
  - **C (vacuous hypothesis): 2** — `LocallyFinite=True` consumed as hyp; `pst_rate_tradeoff` dead args.
  - **E (false/mismatch-as-stated): 2** — Conjecture93 family-vs-realization mismatch; `IsInfinityPST` drops PST content.
  - **F (degenerate quantification): 2** — `levine_base_corollary` one-point quotient; `TopologicalInvarianceConjecture` over `g≥0`.
- **Highest-severity (genuinely misleading green):**
  1. `Tower7.infinity_pst_lift` (hollow via identity `quotient` stub),
  2. `FilteredColimitPST.InversePartitionSequence.pst_lifted` (hollow via `quotient := 0` stub),
  3. `Tower6` PST hierarchy (`StalkwisePST`/`DenseOpenPST` + 3 trivially-proven implications),
  4. Conjecture93 six "satisfies" theorems (rest on two sorried halves; families are cardinality skeletons).
- Note: Tower6/Tower7 honest `sorry`s and clearly-labeled scaffold `Prop`s were
  treated as in-scope only where a *def* is a stub presented as real or a *theorem*
  is green-but-hollow. The bulk of both files is honest scaffolding.
